export const API_PORT = 'NOOP';
export const WS_PORT = 'NOOP';
export const MONGO_HOST = 'NOOP';
export const MONGO_USER = 'NOOP';
export const MONGO_PASSWORD = 'NOOP';
export const MONGO_DATABASE = 'NOOP';
export const IO_NAMESPACE = 'NOOP';

export const KALEIDO_REST_GATEWAY_URL = 'NOOP';
export const KALEIDO_AUTH_USERNAME = 'NOOP';
export const KALEIDO_AUTH_PASSWORD = 'NOOP';
export const OWNER_WALLET = 'NOOP';

export const CONTRACT_CLASS_NAME_RAWDATA = 'NOOP';
export const CONTRACT_CLASS_NAME_PAPERS = 'NOOP';

// Temporarily used to decide if contracts should be deployed.
// Should be set to true when there are contract changes to be deployed,
// then turn back to false once the deployment happened before the next run
export const DEPLOY_CONTRACTS = false;

export const CONTRACT_ADDRESS_RAWDATA = 'NOOP';
export const CONTRACT_ADDRESS_PAPERS = 'NOOP';

export const DATA_HASH_ALGORITHM = 'NOOP';
